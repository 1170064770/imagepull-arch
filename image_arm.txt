# ARM64 / aarch64 container images list (one per line)
# Lines starting with # or empty lines are ignored.
# Whenever this file is changed, the GitHub Action pulls, saves, and creates a release for each image.

alpine:3.20
redis:7.4-alpine
node:22-alpine
