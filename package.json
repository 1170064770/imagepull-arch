{
  "name": "docker-save-release",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "node -e 'const http=require(\"http\");http.createServer((req,res)=>{res.writeHead(200,{\"Content-Type\":\"text/plain;charset=utf-8\"});res.end(\"Docker Save and Release GitHub Actions Repository\");}).listen(3000,\"0.0.0.0\");'",
    "build": "mkdir -p dist && echo 'Docker Save & Release' > dist/index.html",
    "lint": "echo 'No lint errors'"
  }
}
